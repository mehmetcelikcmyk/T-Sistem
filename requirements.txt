# ═══════════════════════════════════════════════════════════════════════════
# T-SISTEM · BAGIMLILIKLAR
# ═══════════════════════════════════════════════════════════════════════════
# Faz 0 temizligi:
#   EKLENDI  : python-docx, groq, Pillow, mammoth, pypdf, argon2-cffi,
#              pydantic-settings, python-dateutil  (kodda import ediliyordu,
#              listede YOKTU — sessiz ImportError uretiyorlardi)
#   CIKARILDI: sentence-transformers, faiss-cpu, chromadb, scikit-learn,
#              langchain*, langdetect, jinja2, tqdm  (~3 GB; kod tabaninda
#              TEK BIR import'lari bile yoktu)
#   BIRLESTIRILDI: pypdf + PyPDF2 ayni isi yapiyordu -> yalnizca pypdf
# ═══════════════════════════════════════════════════════════════════════════

# ── Web / API ──────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-multipart>=0.0.9
requests>=2.31.0
httpx>=0.27.0

# ── Yapilandirma ve dogrulama ──────────────────────────────────────────────
pydantic>=2.6.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1
python-dateutil>=2.9.0

# ── Guvenlik ───────────────────────────────────────────────────────────────
argon2-cffi>=23.1.0            # saltsiz SHA-256 yerine parola hash'i

# ── Belge isleme ───────────────────────────────────────────────────────────
pymupdf>=1.24.0                # PDF render + metin cikarimi
pdfplumber>=0.11.0
pypdf>=4.2.0                   # PyPDF2 yerine (tek paket)
python-docx>=1.1.0             # sablon analizi — ONCEDEN EKSIKTI
mammoth>=1.7.0                 # DOCX -> HTML onizleme — ONCEDEN EKSIKTI
Pillow>=10.3.0                 # logo isleme — ONCEDEN EKSIKTI

# ── Bulut depolama (Cloudflare R2, S3 uyumlu) ──────────────────────────────
boto3>=1.34.0

# ── LLM saglayicilari ──────────────────────────────────────────────────────
anthropic>=0.34.0
openai>=1.23.0
groq>=0.11.0                   # 2. katman — ONCEDEN EKSIKTI (katman oluydu)

# ── Arayuz ─────────────────────────────────────────────────────────────────
streamlit>=1.36.0
plotly>=5.22.0
pandas>=2.2.1
numpy>=1.26.4
reportlab>=4.0.0               # karne PDF uretimi

# ── Test ───────────────────────────────────────────────────────────────────
pytest>=8.1.0

# ── NOT ────────────────────────────────────────────────────────────────────
# Word -> PDF donusumu icin sistem paketi gerekir (pip ile kurulmaz):
#   Debian/Ubuntu : apt-get install -y libreoffice-writer fonts-dejavu fonts-liberation
#   Windows       : Microsoft Word (COM) veya LibreOffice
# Detay: src/services/doc_converter.py
